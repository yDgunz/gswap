function Prop(t_throw, t_catch, flight_path) {
	
	/* initialization for props */
	this.t_throw = t_throw;
	this.t_catch = t_catch;
	this.flight_path = flight_path; // R2L, R2R, L2L, L2R
	this.active = false;
	
}